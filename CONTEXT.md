# usage-hub 领域词典

## 时间预设
定义：用量中心主视图的固定时间范围，包含本年、本月、本周、昨天、今天五项，默认选中今天。
相关概念：用量筛选、Asia/Shanghai 日界

## 会话分组
定义：会话选择列表按今天、昨天、更早三组展示，便于按时间定位会话。
相关概念：会话详情、JSONL 会话

## 会话列表压缩
定义：只压缩会话选择列表的行高和信息密度；会话详情卡片保持正常阅读空间。
相关概念：紧凑列表、会话选择器

## 余额卡
定义：展示已配置厂商的 API 余额，并提供仅针对已配置厂商的筛选。
相关概念：余额监控、供应商

## 额度卡
定义：展示已配置额度计划的供应商额度，并提供仅针对已配置额度计划的筛选。
相关概念：订阅额度、Codex

## 已配置
定义：供应商存在可用于查询的有效凭据或宿主登录配置；未配置的供应商不进入对应卡片和筛选项。
相关概念：余额卡、额度卡、provider-catalog

## 隐藏助手与隐藏模型
定义：设置中分别列出当前数据里的助手和模型，按项独立开关；默认全部显示，隐藏后统一从总览、图表、分布和会话列表中排除。
精确度边界：隐藏**助手**时所有维度均精确排除；隐藏**模型**时总消耗、按模型分布、各 agent 行会排除它，但「来源类型」卡、Provider 分布、小时图仍含其贡献（预聚合无 type×model / hour×dim 交叉表）。
相关概念：设置侧边抽屉、显示偏好、筛选降级

## 小时趋势
定义：只有选择今天或昨天时显示对应日期的小时趋势；选择本周、本月、本年时隐藏小时趋势卡，避免单日数据冒充多日范围统计。
相关概念：时间预设、日趋势

## 侧边当前会话
定义：侧边工具栏中的用量组件只跟随宿主当前焦点会话，展示该会话的标题、模型、轮数、Token、命中率、上下文占用和输入构成，不展示全局用量中心的缩小版。
相关概念：焦点会话、会话 JSONL、widget

## Token 平均速率
定义：Σ输出 token ÷ Σ间隔（加权，非算术平均）。会话口径为相邻 assistant 消息间隔（含工具调用与等待），账本口径为请求 durationMs（含网络与排队）；过滤 100ms~10min；非模型纯生成速度，文本速度已剔除推理 token。
类型归类：生成速度的 type 按会话目录归类（sessions→session、subagent-sessions→subagent、activity/workflow-sessions→automation），其余卡片的 type 按账本子系统归类，两者来源不同。
agent 归属：subagent 条目归真正的子代理（`source.actor.agentId` / 瘦身后的 `attribution.actorAgentId`），其余归 `attribution.agentId`，缺失回退父 agent。
相关概念：加权平均、token-tracker 口径、来源类型、预聚合存储

## 预聚合存储
定义：0.6.0 起的主存储 `rollup.json`，按「天 + 小时 + 维度」预聚合：`days` 覆盖全部历史（体积随天数线性、不随调用数增长）、`recentSessions` 仅最近 30 天、速度样本独立存 `speeds.json`（保留 30 天）。不再保存全量调用明细。首次加载从 `archive.json` 迁移（原名改名备份为 `archive.pre-rollup-<ts>.json`，不删除）。
相关概念：Token 平均速率、筛选降级、命中率

## 筛选降级
定义：预聚合存储只支持「时间 + 单条件」筛选。`from/to` 始终生效；`agent`/`model`/`provider`/`type` 只取第一个非空（优先级 agent→model→provider→type）。按 agent 筛选时分布卡走 agent 交叉表（准确）；按 model/provider/type 筛选时分布卡与小时图显示所选时间范围全量并标注「未随筛选变化」。已放弃：多条件交叉筛选、单次调用明细、延迟分布。
按 agent 筛选口径：该行 `totalTokens` 来自不含隐藏模型的累计，而 `daily.byType` 取自 `byAgent[agent].byType`（含被隐藏模型的贡献）——因此该行 byType 之和可能略大于行 total；日趋势图以 byType 堆叠定 y 轴、不另绘 total，视觉不冲突。
相关概念：预聚合存储、来源类型、时间预设
